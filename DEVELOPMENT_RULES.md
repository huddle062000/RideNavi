# RideNavi 開発基本ルール

このファイルは、RideNaviを開発・調査・修正するときに従う基本ルールを定める。

## 1. 作業開始時の必須確認

RideNaviに関する作業では、コードの調査や変更を始める前に、必ず最初にこの `DEVELOPMENT_RULES.md` を全文確認する。

あわせて、次の内容を確認する。

- ユーザーから指定された作業範囲と、変更してはいけない範囲
- Gitの現在のブランチ、未コミット変更、未追跡ファイル
- 対象機能の既存実装と、それに関連する処理
- 既存コードや既存ファイルを再利用できるか

既存の変更はユーザーの作業として扱い、依頼と無関係な差分を削除、上書き、整形しない。

## 2. RideNaviの開発方針

RideNaviは、一般的なナビアプリをそのまま模倣するのではなく、バイク・ツーリング中に安全で使いやすいナビを目指す。

機能数の多さより、次の価値を優先する。

- 迷わない
- 見やすい
- 操作が少ない
- 走行中でも状況を把握しやすい
- スマートフォンやバイク用端末で使いやすい

機能を追加するときは、画面や操作を不必要に複雑にしない。

## 3. 実装前の調査

変更前に、対象機能だけでなく、その入力と結果を利用する関連処理も確認する。

特に次の既存機能への影響を確認する。

- 現在地取得と地図追従
- 目的地と経由地の設定
- 道路モード
- ルート検索と複数ルート候補
- ナビ開始と終了
- 音声案内と交差点案内
- 自動リルート
- ルート共有
- スマートフォン向け表示

仕様や既存処理が不明なまま、大きな構造変更を行わない。

## 4. 変更ルール

- 依頼された目的を満たすために必要な範囲だけを変更する
- 既存の関数、状態、UI部品、スタイルを可能な限り再利用する
- 依頼されていない機能追加や全面的な書き換えを行わない
- 既存機能との互換性を保つ
- 同じ役割の処理やUIを重複して作らない
- 非同期処理では、古い検索結果や応答が新しい状態を上書きしないようにする
- エラー時や情報取得失敗時にも、利用可能な代替表示や操作手段を残す
- 画面上の操作部品には、必要に応じて適切なラベルやキーボード操作を用意する

ルート検索アルゴリズム、ナビ処理、リルート条件など走行結果に大きく影響する部分は、依頼で明示されている場合に限って変更する。

## 5. UI・スマートフォン対応

UI変更では、スマートフォンの狭い画面とタッチ操作を基準にする。

- 地図を必要以上に隠さない
- 重要な情報を短く、読みやすく表示する
- 走行中の操作回数を増やさない
- ボタンや選択肢は指で押しやすい大きさと間隔にする
- 長い名称や住所でレイアウトが崩れないようにする
- セーフエリアを考慮する
- 表示状態、選択状態、無効状態が分かるようにする

可能な場合は、一般的なスマートフォン幅で実際の表示と操作を確認する。

## 6. API・設定・安全性

- APIキー、認証情報、個人情報を新たにコードへ直接追加しない
- ブラウザで使用する公開APIキーは、提供元で利用APIと許可ドメインを制限する
- 外部APIの失敗、タイムアウト、取得結果なしを考慮する
- 位置情報は必要な目的の範囲でのみ使用する
- 走行中の注視や複雑な操作を促す機能を追加しない

## 7. 作業後の確認

変更内容に応じて、少なくとも次を確認する。

1. JavaScriptの構文チェック
2. 変更差分と意図しないファイル変更の確認
3. 既存機能への影響確認
4. スマートフォン幅での表示確認
5. 主要な操作フローとエラー時の確認

確認できなかった項目は、完了したものとして扱わず、理由とともに報告する。

## 8. Git・公開ルール

- ユーザーから明示的な依頼がない限り、コミット、push、公開、デプロイを行わない
- 強制pushや履歴を破壊する操作を行わない
- 作業前から存在する変更を勝手にコミットへ含めない
- コミットや公開を依頼された場合は、対象差分とテスト結果を確認してから行う

## 9. 作業結果の報告

作業後は、次の内容を簡潔に報告する。

- 変更した内容
- 変更したファイル
- 実施した確認と結果
- 既存機能への影響
- 未確認事項や既知の制約
- コミット、push、公開の実施有無

事実として確認した内容と、推測や今後の提案を区別して記載する。
